info:
  name: Ban
  type: http
  seq: 2

http:
  method: POST
  url: "{{user-url}}/ban"
  body:
    type: json
    data: |-
      {
        "updaterId": 1,
        "targetUserId": 8
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
