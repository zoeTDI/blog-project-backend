info:
  name: Reset Password
  type: http
  seq: 4

http:
  method: POST
  url: "{{user-url}}/resetPassword"
  body:
    type: json
    data: |-
      {
        "code": "273184",
        "newPassword": "123456789"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
