info:
  name: Send Reset Password Email
  type: http
  seq: 3

http:
  method: POST
  url: "{{user-url}}/sendResetPasswordEmail"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
