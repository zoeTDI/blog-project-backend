info:
  name: Create
  type: http
  seq: 1

http:
  method: POST
  url: "{{user-url}}/create"
  body:
    type: json
    data: |-
      {
        "creatorId": 1,
        "username": "auditor",
        "password": "123456",
        "roles": ["AUDITOR"]
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
