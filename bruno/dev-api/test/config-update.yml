info:
  name: config/update
  type: http
  seq: 5

http:
  method: PUT
  url: "{{folder-url}}/config/update"
  body:
    type: json
    data: |-
      {
        "id": 2,
        "configKey": "system.user.init-password",
        "value": "123456",
        "updater": "admin"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
