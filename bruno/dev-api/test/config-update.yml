info:
  name: config/update
  type: http
  seq: 5

http:
  method: PUT
  url: "{{folder-url}}/config/update"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
