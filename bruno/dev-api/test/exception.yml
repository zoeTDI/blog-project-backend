info:
  name: exception
  type: http
  seq: 3

http:
  method: GET
  url: "{{folder-url}}/exception"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
