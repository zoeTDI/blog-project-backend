info:
  name: 激活COS客户端
  type: http
  seq: 7

http:
  method: PUT
  url: "{{folder-url}}/file/switch-master?configId=40"
  params:
    - name: configId
      value: "40"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
