info:
  name: config
  type: http
  seq: 4

http:
  method: GET
  url: "{{folder-url}}/config/get?key=system.user.init-password"
  params:
    - name: key
      value: system.user.init-password
      type: query
    - name: key
      value: not.exist.key
      type: query
      disabled: true
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
