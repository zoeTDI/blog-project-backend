info:
  name: Login 作者
  type: http
  seq: 2

http:
  method: POST
  url: "{{loginEP}}"
  body:
    type: json
    data: |-
      {
        "username": "zhangsan",
        "password": "password123"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
