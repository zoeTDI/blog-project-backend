info:
  name: Login 审核
  type: http
  seq: 3

http:
  method: POST
  url: "{{loginEP}}"
  body:
    type: json
    data: |-
      {
        "username": "lisi",
        "password": "abc123456"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
