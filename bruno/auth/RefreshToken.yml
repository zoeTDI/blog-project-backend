info:
  name: RefreshToken
  type: http
  seq: 2

http:
  method: POST
  url: "{{auth-url}}/refresh"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
