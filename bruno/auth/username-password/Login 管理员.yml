info:
  name: Login 管理员
  type: http
  seq: 1

http:
  method: POST
  url: "{{loginUP}}"
  body:
    type: json
    data: |-
      {
        "username": "admin",
        "password": "123456789"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
