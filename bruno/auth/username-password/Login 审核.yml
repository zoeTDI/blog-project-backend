info:
  name: Login 审核
  type: http
  seq: 1

http:
  method: POST
  url: "{{loginUP}}"
  body:
    type: json
    data: |-
      {
        "username": "auditor",
        "password": "123456"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
