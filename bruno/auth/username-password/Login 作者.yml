info:
  name: Login 作者
  type: http
  seq: 1

http:
  method: POST
  url: "{{loginUP}}"
  body:
    type: json
    data: |-
      {
        "username": "author",
        "password": "123456"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
