info:
  name: ToReview
  type: http
  seq: 11

http:
  method: GET
  url: "{{post-url}}/status/review?postId=52"
  params:
    - name: postId
      value: "52"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
