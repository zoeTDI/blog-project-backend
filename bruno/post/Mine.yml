info:
  name: Mine
  type: http
  seq: 9

http:
  method: GET
  url: "{{post-url}}/blogPost/mine"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
