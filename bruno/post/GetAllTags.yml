info:
  name: GetAllTags
  type: http
  seq: 8

http:
  method: GET
  url: "{{post-url}}/getAllTagsByAuthor"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
